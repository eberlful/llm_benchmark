## Agent skills

### Issue tracker

Local markdown files. See `docs/agents/issue-tracker.md`.

### Triage labels

Default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout. See `docs/agents/domain.md`.


### Code execution

To execute code use the uv which are installed in a conda env called uv-env.

With the command `conda run -n uv-env uv [COMMAND]` you can execute code.

